import React from 'react';

// Define categories with icons (using placeholder SVGs for demonstration)

const Categories = () => {
    return (
        // Added Tailwind classes for responsive grid layout
        <div>
            Categories
        </div>
    );
};

export default Categories;
